import { Route, Routes } from "react-router-dom";

const AppRoutes = () => {
  return (
    <Routes>
      <Route path="/cms">{/* <Route path='/cms/exam' element={<Page/>} /> */}</Route>
    </Routes>
  );
};

export default AppRoutes;
